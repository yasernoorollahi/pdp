# Frontend Understanding (pdp-ui)

## Status
No frontend sources were found in this repository. I searched for typical frontend files (for example `package.json`, `vite.config.*`, `next.config.*`) and none exist here. That means `pdp-ui` is likely in a separate repository or folder.

If you point me to the `pdp-ui` path, I will generate a full frontend deep dive similar to the backend docs.

## What will be documented once pdp-ui is provided

### React architecture
- Application entry point and routing strategy
- Layout patterns (app shell vs. per-page)
- State management (React state, context, external libs)

### Component structure
- Atomic design or feature-based structure
- Shared components vs. feature components
- Styling approach (CSS modules, Tailwind, styled-components, etc.)

### TypeScript basics used
- Types for API responses
- Props/State typing conventions
- Utility types used in the codebase

### API communication
- HTTP client choice (fetch/axios/react-query/etc.)
- Error handling and retry behavior
- Auth token storage and refresh flow

### Design decisions
- Why each pattern is used in your specific UI
- Tradeoffs and risks

## Next step
Provide the location of the `pdp-ui` project, then I will:
1. Map the folder structure.
2. Explain React architecture with code examples.
3. Explain API communication and auth flow.
4. Provide a detailed component deep dive.
